#!/usr/bin/env python

from distutils.core import setup

setup(name='email_template',
    version='0.1',
    description='django email_template extracted from fork of pycon_site',
    author='dvd',
    author_email='dvd@develer.com',
    packages=[
        'email_template',
    ],
    package_data={
    },
    install_requires=[
    ],
)
